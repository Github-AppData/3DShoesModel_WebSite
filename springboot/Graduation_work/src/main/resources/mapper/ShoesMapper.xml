<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.example.rubypaper.mapper.ShoesMapper">

	<select id="randSelectShoesId">
		SELECT shoes_id
		FROM shoes
		ORDER BY RAND()
		LIMIT 12;
	</select>
	
</mapper>